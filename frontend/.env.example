# Frontend Environment Variables

# API Base URL - used for all API calls from the frontend
# In development with separate frontend (port 3000), set this to point to your backend
# REACT_APP_API_BASE_URL=http://localhost:8000/api/v1

# In production or unified mode, leave this commented out to use relative URLs
# REACT_APP_API_BASE_URL=/api/v1

# Example configurations:
# Development (separate frontend/backend): REACT_APP_API_BASE_URL=http://localhost:8000/api/v1
# Production (unified): REACT_APP_API_BASE_URL=/api/v1 (or leave blank)
# Custom deployment: REACT_APP_API_BASE_URL=https://your-domain.com/api/v1
