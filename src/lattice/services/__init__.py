"""
Service layer for the Lattice backend.

This package contains business logic extracted from FastAPI route handlers.
Routes should act as thin controllers that validate inputs, invoke services,
and return responses. This improves testability and separation of concerns.
"""

